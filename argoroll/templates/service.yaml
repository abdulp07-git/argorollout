apiVersion: v1
kind: Service
metadata:
  name: {{ include "argoroll.fullname" . }}
  labels:
    {{- include "argoroll.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argoroll.selectorLabels" . | nindent 4 }}
