apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "argoroll.fullname" . }}-test-connection"
  labels:
    {{- include "argoroll.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "argoroll.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
